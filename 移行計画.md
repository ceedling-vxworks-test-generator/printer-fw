# RIM 移行計画

現行ツリー(`framework/` `devices/` `capi/`)と構成変更ツリー
(`構成変更/librim/RIManager/`)をどう1本にまとめるかの計画。

すべて **実際にビルド・計測して確認した事実** に基づく。推測の箇所はその旨を明記する。

---

## 1. 結論

**構成変更ツリーをベースにし、現行ツリーからは Adapter 層・`RIMDataId` の配置・
install 一式を移植する。**

### 訂正

本書の初版は「現行ツリーをベース」としていた。その後、以下2点が決まったことで
移す量が逆転したため、方針を差し替えた。

- **スレッドモデル: RIM 内で完結**(= 構成変更のワーカ3本方式を採る)
- **Publisher 層: 構成変更側に合わせる**

L2 のスレッドモデルと L3・L4 が構成変更側になると、現行をベースにする場合は
Capability(750行)+ Publisher(813行)+ ワーカ/キュー(153行)に加えて
現行 L2 の作り替えが要る。対して構成変更をベースにするなら Adapter(165行)+
機種規則(約200行)+ `RIMDataId` 移設 + install 一式で済む。

---

## 2. 層ごとの現状比較

| 層 | 現行 | 構成変更 | 採用 |
|---|---|---|---|
| Adapter (L1) | `RawValue` / `DataContext` / `IRule` / `IRuleResolver`。単位正規化(摂氏・華氏→ケルビン)、テーブル方式ディスパッチ | センサ種別ごとに1クラス。core が機種 id を参照。コンパイルできないヘッダ4件 | **現行** → 移植済み |
| Datastore (L2) | `CentralInputPort` + 3レジストリ(CurrentValue / Fault / Operation)+ `RegistryDomain`。単一スレッド | `ValueStore` + `RIMSnapshot` + ワーカ3本 + キュー | **構成変更**(スレッド方式の決定に従う)<br>3レジストリ構造は §5 フェーズ2 で再検討 |
| Capability (L3) | `CapabilitySet` に8種の Cap を `framework/` へ直書き | `CapabilityId` + `CapabilityPayload`(型消去)、規則は products 側 | **構成変更** |
| Publisher (L4) | `PublishEngine` / `SubscriptionBroker` / `StateRepository` | `SubscriberMailbox` / `CallbackSubscriptionRegistry`(いずれも固定容量) | **構成変更**(決定済み) |
| コンテナ | `FixedMap` / `FixedVector` / `Queue` / `RingBuffer` | `FixedVector` / `FixedQueue` | 構成変更 +(必要なら `FixedMap` / `RingBuffer` を移植) |
| C API | `rim_capi.h` + `rim_ids.h`。単位対応、`static_assert` ドリフト検査、試験3種(smoke/value/perf) | `rim_api.h`(id 引数方式)+ トランポリン枠 | **統合** — 全て移植済み(§11) |
| ビルド | install / export / pkg-config / find_package | `-fno-exceptions -fno-rtti` | **両方** — 全て移植済み(§10) |

### 計測値

| 項目 | 現行 | 構成変更(改修前) | 構成変更(改修後) |
|---|---|---|---|
| 定常運転中の動的確保 | 1.0 回/周期 | 3.2 回/周期 | **0 回/周期** |
| 確保箇所 | `CurrentValueBuffer::TakeUpdatedValues()` が `std::vector` を返す 1箇所のみ | Snapshot / ValueStore / キュー3種 / ErrorRepository ほか | なし |

いずれも `operator new` を差し替えて 1000 周期を計測した実測値。

---

## 3. 構成変更ツリー側で埋めるべき差分

初版で「現行の方が進んでいる」として挙げた点は、**すべて構成変更ツリー側へ移植して
解消済み**(§8 参照)。記録として残す。

### 3.1 構成変更ツリーにあった不足(移植済み)

- **`RIMDataId` が core にある**(`core/storage/include/RIMDataId.hpp`)。
  現行では既に `devices/printer_a/inc/datastore/RIMDataId.hpp` へ移してあり、
  `framework/` は機種固有 id を **一切参照していない**(grep で確認済み)。
  構成変更ツリーはこの点で退行している。

- **Adapter が機種知識を持つ**。`core/adapter/include/CelsiusTemperatureAdapter.hpp`
  などセンサ種別ごとにクラスがあり、`RIMDataId::TemperatureAbsolute` を直接参照する。
  現行はこれを `IRuleResolver` + テーブル方式に置き換え済みで、
  規則が100〜1000件に増えても core は無改修。

- **単位正規化が無い**。構成変更ツリーは「摂氏用アダプタ」「ケルビン用アダプタ」と
  **型で単位を分けている**。現行は `DataContext::unit` で受けて1つの規則が正規化する
  (同じ id に摂氏でも華氏でも入れられる)。これは仕様で決めた振る舞いなので、
  構成変更の形に戻すと要件を満たさなくなる。

- **死んだヘッダが4件ある**。単体でコンパイルできない:
  `CelsiusTemperatureAdapter.hpp` / `HumidityAdapter.hpp` /
  `KelvinTemperatureAdapter.hpp`(存在しない `SensorData.hpp` を include)、
  `products/skeleton/Pipeline/SkeletonProvider.hpp`(相対パス誤り)。
  どの `.cpp` からも include されていないため今まで気付かれていない。

- **install / packaging が無い**。現行にはある(`rim.lib` として mainFW へ
  組み込むために作った export / pkg-config / find_package 一式)。

### 3.2 構成変更ツリーが解決した問題は1層に閉じている

構成変更ツリーの成果は実質2つで、いずれも移植できる:

1. **Capability の型消去**(`CapabilityId` + `CapabilityPayload`)と
   core/products 分離。現行の `CapabilitySet`(8種を `framework/` に直書き)を
   置き換える対象がはっきりしている。
2. **固定容量基盤**(`FixedVector` / `FixedQueue`)と `-fno-exceptions -fno-rtti`。
   現行には既に `FixedMap` / `FixedVector` / `Queue` / `RingBuffer` があるので、
   足りないのは `FixedQueue` と、`CurrentValueBuffer` の1箇所の修正だけ。

### 3.3 作業量の見積り(判断後)

| 方向 | 移す対象 | 概算 |
|---|---|---|
| 構成変更 ← 現行(採用) | Adapter(165行)+ 機種規則(約200行)+ `RIMDataId` 移設 + install 一式 | 約450行 |
| 現行 ← 構成変更 | Capability(750行)+ Publisher(813行)+ ワーカ/キュー(153行)+ 現行 L2 の作り替え | 1,700行超 |

---

## 4. 決定済みの事項

### 4.1 スレッドモデル: **RIM 内で完結**(決定済み)

構成変更のワーカ3本(`DataStoreWorker` / `CapabilityWorker` / `PublisherWorker`)を
採る。呼び出し側(mainFW)は `RIManager_Start()` を呼ぶだけでよく、
タスクの起動・停止は RIM が持つ。

現行の「呼び出し側が `Dispatch()` を回す」方式は採らない。

### 4.2 Publisher 層: **構成変更側**(決定済み)

`SubscriberMailbox` / `CallbackSubscriptionRegistry` / `ChangeNotifyManager` を採る。
現行の `PublishEngine` / `SubscriptionBroker` / `StateRepository` は移行しない。

---

## 5. 段階

各フェーズは **単独でビルドが通り、単独でマージできる** 単位に切ってある。
途中で止めても壊れた状態にはならない。

### フェーズ1: Adapter 層の移植 — **完了**(§8)

### フェーズ2: Datastore 層 — **完了**(§9)

**決定: `ValueStore` 1本のままとする。** 現行の3レジストリ
(CurrentValue / Fault / Operation)構造は移行しない。

ただし1本にしたことで `DataContext::faultState` を誰も見ない状態になっていたため、
L2 で異常報告を判別して `ErrorRepository` へ振り分ける経路を追加した。
現行の `FaultRegistry` / `OperationRegistry` / `RegistryDomain` は移行しない。

### フェーズ3: install / packaging の移植 — **完了**(§10)

現行の `rim.lib` 一式(install / export / pkg-config / find_package)を
構成変更ツリーへ持ち込んだ。

### フェーズ4: C API 試験の移植 — **完了**(§11)

現行の smoke / value / perf(C から実際にリンクして実行する試験)を移植した。

### フェーズ5: 後片付け(未着手)

- `構成変更/librim/RIManager/` をリポジトリ直下へ移す
- 旧 `framework/` `devices/` `capi/` を削除
- `old/` の扱いを決める
- 実質空のテスト20件を整理する(82件中20件が3行以下)

## 6. 移行しないもの

| 対象 | 理由 |
|---|---|
| `core/adapter/` のセンサ別アダプタ13件 | 現行の `IRuleResolver` + テーブル方式が上位互換。うち3件はそもそもコンパイルできない |
| `core/adapter/include/rule/` の空スタブ | `DoorSensorRule.hpp` / `HumiditySensorRule.hpp` は TODO コメントのみ |
| `core/storage/include/RIMDataId.hpp` | 現行では devices 側にある(そちらが正) |
| 実質空のテスト20件 | 82件中20件が3行以下(コメントのみ)。移す価値が無い |
| `ExtendedCapabilityManager` 等 | 既に構成変更側で削除済み |

---

## 7. 未解決事項(移行と独立)

移行の前後どちらでも良いが、いずれ決着が要るもの。

- **閾値付近のばたつき**。量子化境界で ±0.0006℃ の雑音により
  1000回の配信で999回の状態反転を観測した。ヒステリシス/デバウンスが要る。
- **範囲外・NaN 入力**が未チェック(未定義動作)。
  int32 は 21,474,836 K で溢れる。これは double の精度限界より 4.19M 倍手前。
- **異常構造体リストの要件**(Phase 14)が未決着。
  置き換え(案A)か併存(案B)かを決めていない。
- **gtest が当環境で取得できない**(ネットワーク制限)。
  構成変更ツリーのテストは構文・型検査までしか確認できておらず、
  **アサーションの実行確認は未実施**。移行時に実行環境を用意すること。

---

## 8. 実施済み(フェーズ1)

構成変更ツリーへ以下を移植し、動作を確認した。

### 移植したもの

| 内容 | 移送先 |
|---|---|
| `RawValue`(スカラ/構造体/配列を非所有参照で受ける) | `core/adapter/include/` |
| `IRule` / `IRuleResolver` / `RawDataInput`(受理点1本) | `core/adapter/include/` |
| `DataContext` / `SourceUnit` / `FaultState` / `RIMResult` | `core/storage/include/` |
| `ICentralInputPort` + `QueueInputPort`(キューへの実装) | `core/storage/` `core/store/` |
| PrinterA の正規化規則(温度・百分率・真偽・計数) | `products/printer_a/DataItem/PrinterARules.hpp` |
| id → 規則のテーブル(`static_assert` 付き) | `products/printer_a/DataItem/PrinterADataProfile.*` |

### 構造の変更

- **`RIMDataId` を core から products へ移した**。core は
  `#include "RIMDataId.hpp"` と名前だけで書き、実体は機種側が供給する。
  core 単体ではビルドが通らないことを実際に確認済み(意図した失敗)。
  番兵 `kCount` と `ToIndex()` は現行ツリーから取り込んだ。
- **`RIManager_Push(id, value, ctx)` を C API に追加**した。
  従来の `RIManager_TestInject*` はキューへ直接積んでおり、
  **規則による正規化を素通り**していた。現在はどちらも Adapter 経由。
- **core → products の include を排除**。規則表の取得も `ProductBinding` の
  `GetProductRuleResolver()` を経由し、リンク時解決にした。
- `PrinterAdapter` は `RIMDataId::kTemperatureSensorA` を直接使うため
  core から products へ移した。

### 削除したもの

- センサ種別ごとのアダプタ8件、空の規則スタブ6件、`AdapterFactory`
- それらを対象にしていた試験4件(新しい Adapter 層の試験に差し替え)

### 追加した試験

`RawDataInputTest`(規則適用・未知 id・変換失敗・context の運搬)、
`RawValueTest`(構造体/配列を**コピーせず参照する**こと)、
`PrinterARulesTest`(摂氏・華氏・未指定、スケール、クランプ)、
`PrinterADataProfileTest`(全 id が規則に対応し、表の並びが崩れていないこと)。

### 副次的に見つけて直したもの

- `products/skeleton/` の雛形が **PrinterA の id を参照していた**。
  そのままでは新機種を起こせないため、skeleton 自身の id へ直し、
  `RIMDataId.hpp` / `SkeletonDataProfile.*` の雛形を追加した。

### 検証

- 全103ソースを `-Wall -Wextra` で警告ゼロ
- `-fno-exceptions -fno-rtti` でライブラリをビルドし、**C から実際にリンクして実行**
  - 摂氏25.0 → 298.150000 K
  - 華氏77.0 → 298.150000 K
  - **差分 0.000000000000**(単位に依らず同じ状態になる)
  - 単位未指定300.0 → 300.000000 K(換算しない)
  - 未知 id → `RI_INVALID_PARAMETER`
- 定常運転中の動的確保 **0回/周期**(Adapter 経由に変えた後も維持)

---

## 9. 実施済み(フェーズ2)

### 決定

**`ValueStore` 1本**。入力の性質ごとにレーンやレジストリを分ける構成は採らない。

### 見つかった問題

1本にしたことで、Adapter が受け取った `DataContext::faultState` を
**誰も見ない**状態になっていた。異常は `RIManager_AddError` という別の入口からしか
入らず、「Adapter の受理点は Push 1本」という方針と食い違っていた。

### 対応

- `FaultApplier`(core/storage)を追加。`faultState` を見て `ErrorRepository` へ
  反映する。発生 / 解除 / 全解除 / 回復状態へ更新 / 発生状態へ更新 に対応。
- `DataStoreWorker`(L2)が項目ごとに判別し、異常報告なら `FaultApplier` へ、
  そうでなければ `ValueStore` へ入れる。**レーンは1本のまま**で、分けているのは
  格納先だけである。異常を `ValueStore` に入れると、Snapshot に異常コードが
  「現在値」として混ざるため。
- 異常コードは `context.key` から取る。取れない場合は値が UInt32/Int32 なら
  そこから取り、どちらも無ければ **何もしない**(0番の異常を勝手に作らない)。
- C API に `rim_fault_state_t` と `has_fault_state` / `has_key` を追加し、
  C++ 側 `rim::FaultState` との `static_assert` ドリフト検査を入れた。
- `RIManager_AddError` / `_RemoveError` / `_SetErrorState` は
  **`Push` に fault_state を添えた場合の短縮形**に書き換えた。
  別実装のままだと重大度の解決などが二重化するため。

### 検証(C から実際に実行)

```
初期状態         : 異常 -1件(Capability 未生成 = RI_NO_DATA)
異常1001を発生   : 異常 1件
異常1002を発生   : 異常 2件
1001を回復状態へ : 異常 2件 / 状態=Recovered(一覧からは消えない)
1001を解除       : 異常 1件
全解除           : 異常 0件
現在値を投入     : 異常 0件(異常一覧は無変化)
```

全104ソースを `-Wall -Wextra` で警告ゼロ。定常運転中の動的確保 0回/周期を維持。

### 移行しないもの(確定)

`FaultRegistry` / `OperationRegistry` / `RegistryDomain` / `IInputClassifier` /
`CurrentValueBuffer` / `CurrentValueDispatcher`。

---

## 10. 実施済み(フェーズ3: install/packaging)

現行ツリーの `rim.lib` 一式を構成変更ツリーへ移植した。

### 移植したもの

- `project()` に `VERSION 0.1.0` を追加
- `GNUInstallDirs` / `CMakePackageConfigHelpers`
- `cmake/rimanagerConfig.cmake.in` / `cmake/rimanager.pc.in`
- `RIMANAGER_IS_TOP_LEVEL` / `RIMANAGER_BUILD_TESTS`(mainFW へ
  `add_subdirectory()` で組み込まれた場合、gtest の FetchContent を
  巻き込まないための既定オフ)

### 実際に `cmake --build` して見つかった問題

これまでの検証は全て `g++` に両方のインクルードパスを手動で渡す形で行っており、
実際に CMake でビルドしたのは今回が初めてだった。その結果、2つの問題が
見つかった。

**1. `rim-core` 単体でビルドが通らない。**
`rim-core` 自身の翻訳単位(`rim_api.cpp` など)は `RIMDataItem.hpp` 経由で
`RIMDataId.hpp`(products 側が供給するヘッダ注入)を必要とするが、CMake は
各ターゲットを自分の include ディレクトリだけでコンパイルするため、
`rim-core` 単体では解決できない。

対応: **どの機種向けにビルドするかを知っているのは最上位の `CMakeLists.txt`
だけ**という前提で、そこから `rim-core` のコンパイルにだけ影響する `PRIVATE`
パスとして products の該当ディレクトリを注入する。`rim-core` の
`CMakeLists.txt` にも C++ コードにも `printer_a` という文字列は一切現れない。

**2. `rim-core.a` と `rim-product-printer-a.a` が互いの未解決シンボルを残す。**
`rim-core` の `rim_api.cpp` は `ProductBinding` 経由で products 側の実装
(`GetProductRuleResolver()` 等)を呼ぶため、2つの静的アーカイブは互いの
シンボルを参照し合う。静的アーカイブのリンクは1パスのみで、素直な順序では
どちらを先に置いても他方の未解決シンボルが残ることを実際のリンクで確認した
(`$<LINK_GROUP:RESCAN,...>` は試したが、生成式の中のターゲット名からは
`INTERFACE_INCLUDE_DIRECTORIES` 等の使用要件が伝播せず、別の失敗を招いた)。

対応: `rim-core` / `rim-product-printer-a` を **OBJECT ライブラリ**に変更し、
両者のオブジェクトを1つの実体 `rimanager`(STATIC)へまとめた。1つのアーカイブ
内の相互参照はインデックス経由で解決できるため、複数アーカイブ間の1パス制限を
受けない。

### 検証

- `find_package(rimanager)` する外部プロジェクトを実際に作成し、ビルド・
  リンク・実行して単位正規化(298.15 K)を確認
- 同じ install 結果を pkg-config 経由(`gcc` 直呼び出し、CMake 不使用)でも
  リンク・実行して確認
- `-fno-exceptions -fno-rtti` が `rimanager::rim-core` の
  `INTERFACE_COMPILE_OPTIONS` として消費側へ正しく伝播することを、
  エクスポートされた `rimanagerTargets.cmake` の中身で確認

---

## 11. 実施済み(フェーズ4: C API 試験の移植)

現行ツリーの smoke / value / perf(C から実際にリンクして実行する試験)を
`test/capi/` へ移植した。

### 追加したもの

- `test/capi/rim_capi_test.h` — 最小テストハーネス(gtest は C から使えない
  ため)。`RIM_EXPECT_NEAR` を追加(温度が固定小数点でなく double になった
  ため)
- `test/capi/rim_capi_test_helpers.h` — push とワーカスレッドの伝播待ちの
  共通処理
- `test/capi/rim_capi_smoke.c` / `rim_capi_value_test.c` / `rim_capi_perf_test.c`
- `products/printer_a/DataItem/rim_data_ids.h` /
  `products/printer_a/CapabilityItem/rim_capability_ids.h` /
  `rim_capability_types.h` — C から Capability・データ id を扱うための
  ミラー(現行ツリーの `rim_ids.h` に相当。無かったので新規に用意した)
- `products/printer_a/Pipeline/PrinterARimIdDriftCheck.cpp` — 上記ミラーと
  C++ 側定義のずれをビルド時に検出する `static_assert` 群(現行ツリーの
  `rim_capi.cpp` にある検査と同じ考え方)

### 見つけた問題1: 非同期ワーカモデルへの対応漏れ

現行ツリーは呼出側が `rim_dispatch()` を明示的に呼ぶ同期モデルだったが、
構成変更ツリーは `RIManager_Start()` でワーカスレッドを3本起動し、push した
値が背後で非同期に伝播する。当初は固定回数の空ループで「待ったつもり」に
していたが、これは2つの意味で不十分だった。

1. **CPU周波数スケーティングの影響**: 同じ反復回数でも実時間が一定せず、
   プロセス内でテストが進むにつれ後方のテストだけ再現性よく詰まる事象を
   実際に確認した。→ 固定時間を信用せず、条件(`HasPendingCapability` /
   `GetCurrentCapability` の成功)を都度確認しながら小刻みに待つ方式へ変更。
2. **既存値の落とし穴**: 同じ Capability へ複数回 push するテスト(境界値を
   1つずつ確かめる等)では、前回 push の値が既に Capability として存在する
   ため「読めたら成功」という条件では **今回の push が反映される前に古い値を
   読んで早期成功してしまう**。→ push する前の値を控えておき、値が
   バイト単位で変わるまで待つ `RimPushAndWaitCurrent()` を実装した(値が
   偶然一致する場合は上限まで待って成功扱いにフォールバックする)。

### 見つけた問題2: `RIManager_GetCapability` がバッファ不足でも通知を消費していた

上記の非同期待ちを直した後もなお、`RIManager_GetCapability` を「小さすぎる
バッファ→`RI_BUFFER_TOO_SMALL`→次に十分なバッファで再取得→成功」という順で
呼ぶテストが失敗し続けた。原因を辿ると、実装は `HasPending()` で存在だけ
確認した後、`TryGet()`(内部で Mailbox から Pop = 消費する)を無条件に呼んで
から `CopyOut()` でサイズを見て `RI_BUFFER_TOO_SMALL` を返していた。
つまり **バッファ不足を返す場合でも、実際には通知を消費していた**。
これはヘッダのドキュメントコメント(「通知は消費しない」)に反する、実装時から
潜在していた不具合で、実際に非同期の往復読み出しをするテストを書いて初めて
検出できた。

対応: `SubscriberMailbox` / `NotificationReceiver` / `RIManager` に
**取り出さずに覗くだけの `Peek()`** を追加し、`RIManager_GetCapability` は
まず `Peek()` でサイズを確認し、収まる場合だけ `TryGet()` で実際に消費する
よう修正した。

### 検証

- 全105ソースを `-Wall -Wextra` で警告ゼロ(C++ 104 + `PrinterARimIdDriftCheck.cpp`)
- C テスト一式を `-Wall -Wextra` で警告ゼロ
- `rim_capi_smoke` を **25回連続実行して全て成功**(修正前は非同期化・
  バッファ不足の扱いの2つの不具合により、実行順や乱数的要因に応じて
  安定して失敗していた)
- `ctest` からも同じ実行ファイルが1テストとして認識され、成功することを確認
- `find_package(rimanager)` 経由で外部プロジェクトからリンクし、
  `-fno-exceptions -fno-rtti` を保ったまま実行できることを再確認
