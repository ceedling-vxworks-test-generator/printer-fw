# VSCode + gdb デバッグ環境構築手順

対象: ホストPC上でネイティブ実行する CMake ベースの C/C++ プロジェクト
(本リポジトリの `.vscode/` は本手順に従って構築済み。動作確認済みのため、
他リポジトリへ同じ構成を持ち込む際のテンプレートとしてそのまま使える)。

---

## 1. 前提条件

| # | 必要なもの | 確認方法 |
|---|---|---|
| 1 | VSCode拡張機能 C/C++(`ms-vscode.cpptools`、Microsoft製) | 拡張機能ビューで検索してインストール。`cppdbg`デバッグタイプを提供する |
| 2 | gdb(Linux)/ lldb(macOS) | `which gdb` |
| 3 | デバッグ情報付きビルド | `CMAKE_BUILD_TYPE=Debug`(`-O0 -g`相当)。CMakeLists.txt側で`-O2`等を強制していないか要確認 |
| 4 | **main関数を持つ実行可能バイナリ** | ライブラリ/オブジェクトのみでは gdb が起動できるプロセスが無いため不可(§4参照) |

`"request": "launch"` はVSCode(gdb)がプロセスを実際に起動する方式のため、
実行ファイルが存在することが前提になる。`"request": "attach"` にしても
「どこかで動いているプロセス」が結局必要なので、この制約からは逃れられない。

---

## 2. `.vscode/tasks.json`

```json
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake: configure (Debug)",
            "type": "shell",
            "command": "cmake",
            "args": [
                "-S", "${workspaceFolder}",
                "-B", "${workspaceFolder}/build-debug",
                "-DCMAKE_BUILD_TYPE=Debug"
                // gtest 等を FetchContent で取得する構成で、かつネットワーク制限がある場合は
                // それをスキップするオプション(例: "-DXXX_BUILD_TESTS=OFF")を追加する
            ],
            "problemMatcher": []
        },
        {
            "label": "cmake: build <TARGET_NAME> (Debug)",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build", "${workspaceFolder}/build-debug",
                "--target", "<TARGET_NAME>"
            ],
            "dependsOn": "cmake: configure (Debug)",
            "group": { "kind": "build", "isDefault": true },
            "problemMatcher": ["$gcc"]
        }
    ]
}
```

`<TARGET_NAME>` はデバッグ対象にしたい実行ファイルの CMake ターゲット名に置き換える。

---

## 3. `.vscode/launch.json`

```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug <TARGET_NAME>",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build-debug/<実行ファイルの出力パス>",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "setupCommands": [
                {
                    "description": "pretty-printingを有効にする",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                },
                {
                    "description": "シェル経由起動を無効化(コンテナ/WSLでの制御端末エラー回避)",
                    "text": "set startup-with-shell off",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "cmake: build <TARGET_NAME> (Debug)"
        }
    ]
}
```

- `set startup-with-shell off` はコンテナ/WSL等で
  `gdb failed to set controlling terminal: Operation not permitted` が出て
  起動できない場合の回避策。素のLinux/macOSでは不要なことが多いので、
  まず無しで試し、エラーが出たら追加する。
- 実行時に共有ライブラリを動的リンクする場合は `environment` に
  `LD_LIBRARY_PATH` を追加する。

---

## 4. main関数(実行可能バイナリ)が無い場合

ライブラリ/オブジェクトしか無い場合、デバッグ用に**最小限のmain()だけのハーネス**を追加する。
網羅的なテストである必要はなく、「止めたい関数を1〜2回呼ぶだけ」で十分。

### `debug/debug_main.c`(テンプレート)

```c
#include <stdio.h>

// デバッグしたい本物のヘッダをここでinclude
// #include "your_module.h"

int main(void)
{
    printf("debug harness start\n");

    // ブレークポイントを張りたい関数をここで呼ぶ。
    // your_function_to_debug(...);

    printf("debug harness end\n");
    return 0;
}
```

### CMakeLists.txt への追加(既存ライブラリ名を `mylib` とした例)

```cmake
add_executable(debug_harness debug/debug_main.c)
target_link_libraries(debug_harness PRIVATE mylib)
```

これで生成される `debug_harness` を `<TARGET_NAME>` として §2/§3 に当てはめる。

本リポジトリでは `test/capi/rim_capi_smoke.c` + `rim_capi_test_main.c` が
同じ構造の実例(実際に動作確認済み)なので、具体的なイメージが欲しい場合は参照する。

---

## 5. あると便利: `.vscode/c_cpp_properties.json`(IntelliSense用・任意)

デバッグ実行自体には不要だが、定義ジャンプ・補完を正しく効かせたい場合に追加する。

```json
{
    "configurations": [
        {
            "name": "Linux",
            "compileCommands": "${workspaceFolder}/build-debug/compile_commands.json",
            "cStandard": "c17",
            "cppStandard": "c++17"
        }
    ],
    "version": 4
}
```

利用するには §2 の configure タスクに `-DCMAKE_EXPORT_COMPILE_COMMANDS=ON` を追加する。

---

## 6. フォルダ構成変更への追従について

`tasks.json`/`launch.json` は個々のソースファイルパスを一切参照しないため、
`.cpp`/`.hpp` の移動・リネームでは壊れない。**壊れる/直す必要があるのは次の場合のみ**:

| ケース | 影響 |
|---|---|
| ターゲット名を変更した | `tasks.json` の `--target` と `launch.json` の `program` |
| 実行ファイルの**出力先**が変わった | `launch.json` の `program` パス。CMakeはターゲットを定義した `CMakeLists.txt` のあるサブディレクトリへ出力をミラーするため、ターゲット定義を別フォルダへ移すと出力パスも変わる |
| デバッグ対象の実行ファイルが複数ある | `launch.json` の `configurations` をターゲットの数だけ用意する必要がある |

**対策**: トップレベルの `CMakeLists.txt` に以下を1行加えておくと、
ターゲット定義がどのサブフォルダに移動しても出力先が固定され、
`program` パスが変わらなくなる。

```cmake
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin)
```

---

## 7. 動作確認手順

VSCode経由で切り分けにくい問題を避けるため、まず素のgdbで確認してから
VSCodeに進む。

```sh
# 1. Debugビルド
cmake -S . -B build-debug -DCMAKE_BUILD_TYPE=Debug
cmake --build build-debug --target <TARGET_NAME>

# 2. stripされていないことを確認
file build-debug/<実行ファイル>   # "not stripped" になっていること

# 3. 素のgdbでブレークポイントが効くか確認
gdb -q --batch \
  -ex "break <関数名>" \
  -ex "run" \
  -ex "bt 5" \
  ./build-debug/<実行ファイル>
```

ここで意図した箇所で停止し、バックトレースが取得できれば、
VSCodeの `cppdbg`(内部的にはgdbを同じように呼び出す)でも同様に動作する。
