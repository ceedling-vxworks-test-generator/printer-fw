# 開発運用ルール(2026-08-27〜)

## 日付フォルダでの開発

これ以降の開発は、リポジトリ直下に置かれる **`MMDD_FW/`** 形式の日付フォルダ内で
直接行う(例: `0820_FW/`)。フォルダはユーザーが随時手動で追加する。

- **デフォルトの開発対象は、存在する日付フォルダのうち最新のもの。**
  指示に対象日付の明示がなければ、最新の`MMDD_FW/`を対象とする。
- ユーザーから過去の日付が明示された場合は、その日付のフォルダを対象にする
  (例:「0820のPublisherを直して」→ `0820_FW/`配下を編集する)。
- 日付フォルダの内部構成は、既存の`0820_FW/`のように1階層下に`rim/`が
  入れ子になっている場合がある(`0820_FW/rim/core/...`、
  `0820_FW/rim/CMakeLists.txt`等)。対象フォルダごとに実際の構成を
  確認してから作業すること(ルート直下と同じ階層とは限らない)。
- 旧来の「日付フォルダから1つを選んでルート直下(`core/`/`products/`/`test/`等)へ
  昇格・同期する」運用は廃止した。今後、日付フォルダの内容をルートへコピーする
  操作は行わない。

## ルート直下(`core/`・`products/`・`test/`・`include/`・`samples/`・
`CMakeLists.txt`等)の扱い

**2026-08-27時点の「0827」マスタを最後に凍結し、以後は参照専用。** 特別な指示が
ない限り編集しない。ビルド確認や過去の実装を参照する目的での読み取りは問題ない。

`old_0818/`・`old_0820/`は、それぞれ0818・0820時点でルート直下に置かれていた
旧マスタの控え(参照専用のアーカイブ)。`0820_FW/`はルートへ昇格されなかった
参照用スナップショット。いずれも新規開発の対象ではない。
