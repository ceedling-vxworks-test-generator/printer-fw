# 開発運用ルール(2026-08-27〜、フォルダ命名は2026-08-31改訂)

## Revフォルダでの開発

これ以降の開発は、リポジトリ直下に置かれる **`Rev○○_FW/`** 形式のフォルダ内で
直接行う(例: `Rev755_FW/`)。フォルダはユーザーが随時手動で追加する。

- **2026-08-31以降、命名規則を日付ベースの`MMDD_FW/`から`Rev○○_FW/`
  (リビジョン番号ベース)へ変更した。** 例えば8/31に作業していたフォルダは
  `0831_FW/`から`Rev755_FW/`へ改名されている。
  `0818/`・`0820/`・`0820_FW/`・`0827/`は改名前(日付ベース)の旧フォルダで、
  過去の参照用としてそのまま残る。
- **デフォルトの開発対象は、存在する`Rev○○_FW/`フォルダのうち最新のもの
  (原則リビジョン番号が最大のもの)。** 指示に対象の明示がなければ最新の
  `Rev○○_FW/`を対象とする。
- ユーザーから過去のリビジョンが明示された場合は、そのフォルダを対象にする
  (例:「Rev755のPublisherを直して」→ `Rev755_FW/`配下を編集する)。
- フォルダの内部構成は、`0820_FW/rim/core/...`のように1階層下に`rim/`や
  `RIManager/`が入れ子になっている場合がある。対象フォルダごとに実際の構成を
  確認してから作業すること(ルート直下と同じ階層とは限らない)。
- 旧来の「フォルダから1つを選んでルート直下(`core/`/`products/`/`test/`等)へ
  昇格・同期する」運用は廃止した。今後、Revフォルダの内容をルートへコピーする
  操作は行わない。

## ルート直下(`core/`・`products/`・`test/`・`include/`・`samples/`・
`CMakeLists.txt`等)の扱い

**2026-08-27時点の「0827」マスタを最後に凍結し、以後は参照専用。** 特別な指示が
ない限り編集しない。ビルド確認や過去の実装を参照する目的での読み取りは問題ない。

`old_0818/`・`old_0820/`は、それぞれ0818・0820時点でルート直下に置かれていた
旧マスタの控え(参照専用のアーカイブ)。`0820_FW/`はルートへ昇格されなかった
参照用スナップショット。いずれも新規開発の対象ではない。
