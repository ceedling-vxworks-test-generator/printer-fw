# RIM_AdapterLayer 詳細設計書 (RIM_new)

クラス図は `03_クラス図.md`、シーケンス図は `04_シーケンス図.md` を参照。

## 1. コンポーネント

| コンポーネント | 所属 | 役割 |
|----------------|------|------|
| `RawDataInput` | framework | 受理点。唯一の公開API `Push(id, RawValue, ctx?)` でRule選択→変換→`Post` を実行(性質判別はしない) |
| `RawValue` | framework | 受理点の型自由な生値(スカラ / 構造体・配列のバイト列参照) |
| `IRule` | framework | `Convert(RawValue, Context) → optional<RIMValue>`。正規化規則の抽象 |
| `IRuleResolver` | framework | `SelectRule(id) → const IRule*`。id→Rule 解決の抽象(devices実装) |
| `IInputClassifier` | framework | `Classify(id) → optional<InputKind>`。id→性質 の抽象(devices実装)。**L2が使用**(Adapterは非依存) |
| `ICentralInputPort` | framework(L2提供) | `Post(RIMDataItem)`。送出先の抽象(1本。振り分けは実装側=L2の責務) |
| 具象 Rule 群 | devices | `TemperatureRule` / `PercentRule` / … 実際の正規化(§2.1.1) |
| `PrinterADataProfile` | devices | `IRuleResolver` + `IInputClassifier` 実装。id→(Rule, 性質) の対応表(`kTable`)への参照窓口 |
| `RIMDataId` | devices | id一覧(型・`ToIndex`・`kRIMDataIdCount`)。機種固有。§1.1参照 |

### 1.1 RIMDataId の所在(machine-agnostic framework の要点)

`RIMDataId` は機種ごとにデータ種別が異なるため、**devices側**
(`devices/printer_a/inc/datastore/RIMDataId.hpp`)で定義する。framework の各ヘッダは
`#include "datastore/RIMDataId.hpp"` という**ヘッダ名にのみ**依存し、`devices/...` への
物理パスを一切知らない。CMakeLists.txt が `framework/inc` と `devices/printer_a/inc` の
両方をインクルードパスに積むことで、framework 側の `#include` が devices 側の実体解決に
届く(ヘッダインジェクション。FreeRTOSConfig.h 等と同じパターン)。

新機種を追加するときは `devices/<機種>/inc/datastore/RIMDataId.hpp` を用意して
ビルド対象のインクルードパスを差し替えるだけで、framework のソースは一切変更不要。
雛形は `devices/_skeleton/inc/datastore/RIMDataId.hpp` を参照。

## 2. データ型

### 2.1 RawValue(IN)
```cpp
struct RawValue {
    enum class Kind { kScalar, kBytes };
    Kind        kind;
    double      scalar;   // kScalar
    const void* data;     // kBytes: 構造体/配列の先頭(非所有)
    size_t      size;     // kBytes: 総バイト長
    size_t      count;    // kBytes: 要素数(配列=n, 構造体=1)
    static RawValue Scalar(double);
    template<class T> static RawValue Struct(const T&);       // data=&s, count=1
    template<class T> static RawValue Array(const T*, size_t);// data=a, count=n
    template<class T> const T* As() const;                    // 先頭を T* として解釈
    template<class T> const T& ElementAt(size_t i) const;     // 配列要素
};
```

### 2.1.1 DataContext(単位による分岐)

```cpp
enum class SourceUnit { kNormalized = 0, kCelsius, kFahrenheit };
struct DataContext {
    std::optional<SourceUnit>    unit;        // 生値の単位。未指定=kNormalized(換算しない)
    std::optional<FaultState>    faultState;
    std::optional<std::int32_t>  scaleX1000;
    std::optional<std::uint32_t> key;
};
```

同一 id へ複数の単位で値が届く場合、**id を増やさず context で分岐**する。
Rule が `ctx.unit` を見て内部統一表現へ換算するため、後続層は単位を意識しない。

`TemperatureRule`(PrinterA)の例:

| `ctx.unit` | 換算 |
|---|---|
| `kCelsius` | K = C + 273.15 |
| `kFahrenheit` | K = (F − 32) × 5/9 + 273.15 |
| `kNormalized` / 未指定 | 既にケルビンとみなしそのまま採用(**換算しない**) |
| 上記以外 | この id に無関係な単位 → `nullopt`(→ `kErrConvert`) |

> **丸め**: 華氏→摂氏の `(F−32)×5/9` は割り切れないため、`TemperatureRule` のみ
> 四捨五入(`std::lround`)する。他の Rule は 0方向への切り捨て。
> 例) 100°F = 310.9277…K → `31093`(切り捨てなら `31092`)。

### 2.2 RIMDataItem(OUT)
```cpp
struct RIMDataItem { RIMDataId id; RIMValue value; DataContext context; };
```

## 3. in → out の経路(処理フロー)

`RawDataInput::Push(id, RawValue, ctx)` が中核(公開APIはこの1つのみ)。

```
Push(id, raw, ctx)                        // raw は呼出側が RawValue::Scalar/Struct/Array で組立済み
  1. resolver.SelectRule(id)              // Rule 未定義 → kErrConvert
  2. item.id = id; item.context = *ctx
  3. rule.Convert(RawValue, ctx)          // 正規化。失敗 → kErrConvert
     → item.value = RIMValue
  4. port.Post(item)                      // 以降(性質判別・レーン振り分け)は L2 の責務
     └ L2: Classify(id) → Fault/Operation/CurrentValue の各レーンへ
            (性質不明 → kErrClassify、値型不一致 → kErrKindMismatch、満杯 → kErrPost)
```

- スカラは呼出側で `RawValue::Scalar(double)` を組み立てて渡す(int32/uint32はdoubleへ暗黙変換)。
- 構造体は `RawValue::Struct(s)`(バイト列参照、count=1)。
- 配列は `RawValue::Array(a, n)`(バイト列参照、count=n)。
- **RawValue は Push 呼び出し中のみ有効**(呼び出し側が実体を所有)。`Convert` は同期的に
  `RIMValue`(値コピー) を生成するため、post までに参照は解決済み。

## 4. 新規ルールを追加する流れ

外部から新しいデータ種別(または新しい入力形式)を受けたいとき:

1. **id を用意**: `devices/printer_a/inc/datastore/RIMDataId.hpp` に列挙子を追加(`kCount` の手前)。
   ※ 既存 id を別形式で受けたいだけなら不要。
2. **Rule を実装**: `IRule` を実装するクラスを devices 側に追加。
   `Convert(RawValue, ctx)` で RawValue(スカラ/構造体/配列) を `RIMValue` へ正規化する。
   ```cpp
   class MyRule final : public IRule {
     std::optional<RIMValue> Convert(const RawValue& raw, const DataContext& ctx) const override {
       // 構造体例: auto* p = raw.As<MyPacket>(); return RIMValue::Percent(p->level);
       // 配列例  : sum over raw.ElementAt<uint8_t>(i) ... return RIMValue::Percent(avg);
     }
   };
   ```
3. **Rule 実体を用意**: `PrinterADataProfile.cpp` の匿名namespaceに `const MyRule kMyRule;` を追加
   (Ruleは状態を持たないため、id横断で共有する単一インスタンスとして置く)。
4. **テーブルに1行追加**: 同ファイルの `kTable` に `{ RIMDataId::kMyId, &kMyRule, InputKind::kXxx }` を追記する。
   `SelectRule`(Rule選択)と `Classify`(性質判別)の両方がこの1行から解決されるため、
   旧来のように2つのswitchへ別々に追記する必要はない。
5. **受理**: 呼び出し側は `Push(id, RawValue::Struct/Array/Scalar(...))` でそのまま投入できる(Adapter変更不要)。
6. **テスト**: `IRule` 単体 + `RawDataInput`(モック port) で検証(§6)。
   `PrinterADataProfileTest`(`test/unit/PrinterADataProfileTest.cpp`)に期待値を追記し、
   行の欠落・取り違えを回帰テストで検知できるようにする。

> 変更は「id 追加 + Rule 追加 + `kTable` への1行追記」に局所化される。
> `kTable` の行数は `static_assert` で `RIMDataId` の総数と自動照合されるため、
> 行を追加し忘れると(id側だけ増やすと)ビルドが失敗する。framework(共通機構)には触れない。

## 5. DataStore で新しいデータを管理する流れ

Adapter が送出した `RIMValue` を L2 でどう保持・提供するか。**性質(kind)により経路が異なる**。

### 5.1 最新値系(CurrentValue) — 最も一般的
1. `CentralInputPort.Post(item)` が `Classify(id) = kCurrentValue` と判別 → `CurrentValueBuffer`。
2. `CurrentValueRegistry::Apply(item)` が `DomainForId(id)` で**副ドメイン**を判定して格納。
   `DomainForId` はテーブル駆動(`CurrentValueRegistry.cpp` の `kDomainTable`)。
   → 新 id を既存副ドメイン(Environment/Consumable/Safety/Maintenance/Health)に割り当てるなら
   `kDomainTable` に1行追加するだけ(行数は `static_assert` で `RIMDataId` の総数と自動照合)。
3. `MakeSnapshot(domains)` の該当副Snapshot(例 `EnvironmentSnapshot`)にフィールドを追加して露出。
4. Capability 側(`ICapabilityBuilder`)がそのフィールドを参照して判定する。

### 5.2 異常系(Fault) / 動作報告系(OperationReport)
- Fault: `Post`(Classify=kFault) → `FaultInputQueue` → `FaultDispatcher` → `FaultRegistry`(FixedMap で add/remove/update)。
- Operation: `Post`(Classify=kOperationReport) → `OperationReportQueue` → `OperationDispatcher` → `OperationRegistry`。

### 5.3 新しい**副ドメイン**や**新しい Registry** が必要な場合
- 新副ドメイン: `RegistryDomain.hpp` にビットを追加 → `CurrentValueRegistry.cpp` の
  `kDomainTable` と `MakeSnapshot` に対応 → `Snapshot.hpp` に副Snapshot 追加 → `MachineRegistry.Capture` の
  `kDomainCurrentAll` に含める。
- 構造化値をそのまま保持したい場合: `ValueType` に新種別を追加し、`RIMValue` に対応する
  格納表現(例: 固定長バッファ)を足す。ただし Adapter の原則は「正規化して単純値で渡す」ため、
  まずは既存の `RIMValue` 種別へ落とせないかを検討する。

> 責務境界: Adapter は「正規化して post するだけ」。**どのドメインに保持し、どう Snapshot 化するかは
> L2 の責務**。Adapter 側の変更(§4)と L2 側の変更(§5)は独立して行える。

## 6. Adapter 単体テスト方針

Adapter は `ICentralInputPort` にのみ依存するため、**L2 を組まずに単体検証**できる。

- **モック port**: `ICentralInputPort`(`Post`1本)を実装し、post された `RIMDataItem` を
  記録する `MockPort` を用意する。Adapter は振り分けをしないため、レーン別ではなく単一列で記録する。
- **テスト用 Resolver**: scalar / array / struct の Rule と、それらを id へ割り当てる
  `IRuleResolver` をテスト内に定義する(`IInputClassifier` は Adapter の依存ではないため不要)。
- **検証観点(Adapter)**:
  - スカラ/構造体/配列 各 Push が正しい `RIMValue` へ正規化されること。
  - `id` / `context` が加工されず素通しされること。
  - Rule 未定義 id → `kErrConvert`、型不一致(scalar用へ構造体) → `kErrConvert`。
  - `Post` の戻り値(L2のエラー)が呼出元へ透過すること。
- **検証観点(L2 = 振り分け)**: 性質判別とレーン振り分け、値型検証、満杯時の `kErrPost` は
  `CentralInputPort` の責務なので `test/unit/CentralInputPortTest.cpp` で検証する。

実装は `test/unit/AdapterTest.cpp`(MockPort + TestProfile + scalar/array/struct Rule)と
`test/unit/CentralInputPortTest.cpp`(実 Queue/Buffer + TestClassifier)を参照。

## 7. エラー処理(RIMResult)

| 結果 | 契機 |
|------|------|
| `kOk` | 正常に post された |
| `kErrConvert` | Rule 未定義、または Convert が nullopt(型不一致・変換不能) |
| `kErrClassify` | Classify が性質を返せない(L2が返す) |
| `kErrKindMismatch` | (L2側)post種別と id 分類/値型の不一致 |
| `kErrPost` | (L2側)Queue 満杯=喪失 |
| `kErrInvalidArg` | id 範囲外等 |
